# 本地配置覆盖示例
# 复制为 .env.local 并填写真实值
# 此文件已加入 .gitignore，不会提交

# 自定义工具目录（默认已由 ext_bridge.py 处理，通常不需要设置）
# EXTRA_TOOLS_DIR=/path/to/custom/tools

# 覆盖任何上游 .env 中的配置
# OPENAI_API_KEY=your_key_here
# LLM_MODEL=gpt-4o
